diff --git a/criu/include/shmem.h b/criu/include/shmem.h
index 15cab11..7594cb6 100644
--- a/criu/include/shmem.h
+++ b/criu/include/shmem.h
@@ -7,8 +7,17 @@
 #include "pagemap-cache.h"
 
 struct vma_area;
+struct list_head;
 
 extern int collect_shmem(int pid, struct vma_area *vma);
+/*
+ * Restore the contents of this task's own shmem objects on a thread pool,
+ * ahead of the serial VMA walk. Best-effort: anything it does not claim, or
+ * fails on, open_shmem() still handles inline.
+ */
+extern int shmem_restore_parallel(int pid, struct list_head *vmas);
+/* How many objects the restore-side thread pools work on at once. */
+extern unsigned int shmem_restore_threads(void);
 extern int collect_sysv_shmem(unsigned long shmid, unsigned long size);
 extern int cr_dump_shmem(void);
 extern int add_shmem_area(pid_t pid, VmaEntry *vma, pmc_t *pmc);
diff --git a/criu/mem.c b/criu/mem.c
index 9e8740c..4089fbd 100644
--- a/criu/mem.c
+++ b/criu/mem.c
@@ -1456,6 +1456,15 @@ int open_vmas(struct pstree_item *t)
 
 	filemap_ctx_init(false);
 
+	/*
+	 * Best-effort pre-pass: fill this task's shared anonymous mappings on
+	 * several threads, so the walk below only has to adopt them. Anything
+	 * it does not manage is restored inline by vm_open() exactly as
+	 * before, so a failure here is not one worth aborting the restore for.
+	 */
+	if (shmem_restore_parallel(pid, &vmas->h))
+		pr_warn("Parallel shmem restore unavailable; restoring inline\n");
+
 	list_for_each_entry(vma, &vmas->h, list) {
 		if (!vma_area_is(vma, VMA_AREA_REGULAR) || !vma->vm_open)
 			continue;
diff --git a/criu/shmem.c b/criu/shmem.c
index bc7aa36..5cf5d85 100644
--- a/criu/shmem.c
+++ b/criu/shmem.c
@@ -3,6 +3,7 @@
 #include <stdlib.h>
 #include <fcntl.h>
 #include <stdbool.h>
+#include <pthread.h>
 
 #include "common/config.h"
 #include "common/list.h"
@@ -68,6 +69,22 @@ struct shmem_info {
 			 */
 			int fd;
 
+			/*
+			 * A memfd already created and filled by
+			 * shmem_restore_parallel(), for open_shmem() to
+			 * adopt. -1 means nothing prepared, -2 means claimed
+			 * but not yet filled.
+			 *
+			 * Unlike the memfd inode's equivalent this may be a
+			 * plain descriptor: shmem_restore_parallel() runs
+			 * from open_vmas(), i.e. already inside the forked
+			 * task that will consume it, so it never crosses a
+			 * fork. (prepare_memfd_inodes() runs in CRIU's main
+			 * process, before the tasks exist, which is why that
+			 * side has to go through the fdstore.)
+			 */
+			int prep_fd;
+
 			/*
 			 * 0. lock is initialized to zero
 			 * 1. the master opens a descriptor and set lock to 1
@@ -440,6 +457,7 @@ int collect_shmem(int pid, struct vma_area *vma)
 	si->pid = pid;
 	si->size = size;
 	si->fd = -1;
+	si->prep_fd = -1;
 	si->count = 1;
 	si->self_count = 1;
 	futex_init(&si->lock);
@@ -543,6 +561,262 @@ out:
 	return ret;
 }
 
+/*
+ * Parallel shmem restore.
+ *
+ * open_vmas() walks a task's VMAs one at a time, and for each shared
+ * anonymous mapping open_shmem() does the whole of create -> resize -> map ->
+ * read -> next before the walk moves on. Every one of those reads is a
+ * synchronous pread against the pages image, so a task with many shmem
+ * objects restores them strictly one after another and leaves most of the
+ * device's read bandwidth unused.
+ *
+ * Distinct shmem objects have no dependency on each other, so that ordering
+ * costs nothing to give up. This pass fills them up front on a pool of
+ * threads and leaves the memfd on si->prep_fd; open_shmem() adopts it and
+ * skips to the part that does have ordering constraints -- publishing si->fd
+ * and the futex handshake with the slave processes, which stays exactly as it
+ * was, on this task's own thread.
+ */
+
+#define SHMEM_RESTORE_THREADS_MAX 32
+
+struct shmem_prep_job {
+	struct shmem_info *si;
+	int ret;
+};
+
+struct shmem_prep_pool {
+	struct shmem_prep_job *jobs;
+	unsigned int nr_jobs;
+	unsigned int next;
+	pthread_mutex_t lock;
+};
+
+/*
+ * How many objects to restore at once. Shared with the memfd pass, which has
+ * the same shape of work and no reason to be tuned separately.
+ * CRIU_SHMEM_RESTORE_THREADS overrides the default, and 1 turns both passes
+ * off entirely -- which is what makes an A/B against the unpatched behavior
+ * possible on one binary.
+ */
+unsigned int shmem_restore_threads(void)
+{
+	const char *env = getenv("CRIU_SHMEM_RESTORE_THREADS");
+	long nr = 0;
+
+	if (env && *env) {
+		char *end;
+
+		nr = strtol(env, &end, 10);
+		if (*end || nr < 1) {
+			pr_warn("Ignoring bad CRIU_SHMEM_RESTORE_THREADS=%s\n", env);
+			nr = 0;
+		}
+	}
+
+	if (!nr) {
+		nr = sysconf(_SC_NPROCESSORS_ONLN);
+		if (nr < 1)
+			nr = 1;
+		if (nr > 8)
+			nr = 8;
+	}
+
+	if (nr > SHMEM_RESTORE_THREADS_MAX)
+		nr = SHMEM_RESTORE_THREADS_MAX;
+
+	return (unsigned int)nr;
+}
+
+/*
+ * Fill one memfd with its checkpointed contents. Everything here is
+ * per-object: the memfd, its mapping, and the page_read that
+ * restore_shmem_content() opens against this object's own image files. No two
+ * workers touch the same shmem_info.
+ */
+static int shmem_prep_one(struct shmem_info *si)
+{
+	void *addr;
+	int f;
+
+	f = memfd_create("", 0);
+	if (f < 0) {
+		pr_perror("Unable to create memfd for shmem 0x%lx", si->shmid);
+		return -1;
+	}
+
+	if (ftruncate(f, si->size)) {
+		pr_perror("Unable to truncate memfd for shmem 0x%lx", si->shmid);
+		close(f);
+		return -1;
+	}
+
+	addr = mmap(NULL, si->size, PROT_WRITE | PROT_READ, MAP_SHARED | MAP_FILE, f, 0);
+	if (addr == MAP_FAILED) {
+		pr_perror("Can't mmap shmem 0x%lx size=%ld", si->shmid, si->size);
+		close(f);
+		return -1;
+	}
+
+	if (restore_shmem_content(addr, si) < 0) {
+		pr_err("Can't restore shmem 0x%lx content\n", si->shmid);
+		munmap(addr, si->size);
+		close(f);
+		return -1;
+	}
+
+	munmap(addr, si->size);
+	si->prep_fd = f;
+	return 0;
+}
+
+static void *shmem_prep_worker(void *arg)
+{
+	struct shmem_prep_pool *pool = arg;
+
+	while (1) {
+		struct shmem_prep_job *job;
+
+		pthread_mutex_lock(&pool->lock);
+		if (pool->next >= pool->nr_jobs) {
+			pthread_mutex_unlock(&pool->lock);
+			break;
+		}
+		job = &pool->jobs[pool->next++];
+		pthread_mutex_unlock(&pool->lock);
+
+		job->ret = shmem_prep_one(job->si);
+	}
+
+	return NULL;
+}
+
+static void shmem_unclaim(struct list_head *vmas)
+{
+	struct vma_area *vma;
+
+	list_for_each_entry(vma, vmas, list) {
+		struct shmem_info *si;
+
+		if (!vma_area_is(vma, VMA_AREA_REGULAR) || vma->vm_open != open_shmem)
+			continue;
+		si = shmem_find(vma->e->shmid);
+		if (si && si->prep_fd == -2)
+			si->prep_fd = -1;
+	}
+}
+
+/*
+ * Restore, in parallel, the contents of every shmem object this task owns and
+ * will have to create itself.
+ *
+ * Deliberately conservative about what it claims. Hugetlb mappings and the
+ * no-memfd fallback both need open_shmem()'s original path, and an object
+ * another task owns is that task's job -- anything not claimed here is simply
+ * left for open_shmem() to do inline, so the worst case is the old behavior
+ * rather than a broken restore. A failure is not fatal either: the object
+ * goes back to prep_fd == -1 and open_shmem() retries it inline, where the
+ * error is reported in the context that has always reported it.
+ */
+int shmem_restore_parallel(int pid, struct list_head *vmas)
+{
+	struct shmem_prep_pool pool = {};
+	pthread_t threads[SHMEM_RESTORE_THREADS_MAX];
+	unsigned int nr_threads, i, started = 0;
+	struct vma_area *vma;
+
+	if (!kdat.has_memfd)
+		return 0;
+
+	nr_threads = shmem_restore_threads();
+	if (nr_threads < 2)
+		return 0;
+
+	/* Count first, so the job array can be allocated in one go. */
+	list_for_each_entry(vma, vmas, list) {
+		struct shmem_info *si;
+
+		if (!vma_area_is(vma, VMA_AREA_REGULAR) || vma->vm_open != open_shmem)
+			continue;
+		if (vma->e->flags & MAP_HUGETLB)
+			continue;
+
+		si = shmem_find(vma->e->shmid);
+		if (!si || si->pid != pid || si->fd != -1 || si->prep_fd != -1)
+			continue;
+
+		/*
+		 * Claim it now, so a second VMA onto the same object does not
+		 * queue it twice. shmem_prep_one() overwrites this with the
+		 * real descriptor.
+		 */
+		si->prep_fd = -2;
+		pool.nr_jobs++;
+	}
+
+	if (pool.nr_jobs < 2) {
+		/* Nothing to overlap; undo the claims and let open_shmem work. */
+		shmem_unclaim(vmas);
+		return 0;
+	}
+
+	pool.jobs = xzalloc(pool.nr_jobs * sizeof(*pool.jobs));
+	if (!pool.jobs) {
+		shmem_unclaim(vmas);
+		return -1;
+	}
+
+	pool.nr_jobs = 0;
+	list_for_each_entry(vma, vmas, list) {
+		struct shmem_info *si;
+
+		if (!vma_area_is(vma, VMA_AREA_REGULAR) || vma->vm_open != open_shmem)
+			continue;
+
+		si = shmem_find(vma->e->shmid);
+		if (!si || si->prep_fd != -2)
+			continue;
+
+		pool.jobs[pool.nr_jobs].si = si;
+		pool.jobs[pool.nr_jobs].ret = -1;
+		pool.nr_jobs++;
+	}
+
+	if (nr_threads > pool.nr_jobs)
+		nr_threads = pool.nr_jobs;
+
+	pr_info("Restoring %u shmem objects on %u threads\n", pool.nr_jobs, nr_threads);
+	pthread_mutex_init(&pool.lock, NULL);
+
+	for (i = 0; i < nr_threads; i++) {
+		if (pthread_create(&threads[i], NULL, shmem_prep_worker, &pool)) {
+			pr_perror("Can't start shmem restore thread %u", i);
+			break;
+		}
+		started++;
+	}
+
+	/* This thread is one more pair of hands, and covers started == 0. */
+	shmem_prep_worker(&pool);
+
+	for (i = 0; i < started; i++)
+		pthread_join(threads[i], NULL);
+
+	pthread_mutex_destroy(&pool.lock);
+
+	for (i = 0; i < pool.nr_jobs; i++) {
+		if (pool.jobs[i].ret == 0)
+			continue;
+		pr_warn("Parallel restore of shmem 0x%lx failed; falling back to inline restore\n",
+			pool.jobs[i].si->shmid);
+		pool.jobs[i].si->prep_fd = -1;
+	}
+
+	xfree(pool.jobs);
+	return 0;
+}
+
 struct open_map_file_args {
 	unsigned long addr, size;
 };
@@ -593,6 +867,17 @@ static int open_shmem(int pid, struct vma_area *vma)
 		memfd_flag |= MFD_HUGETLB | size_flag;
 	}
 
+	if (si->prep_fd >= 0) {
+		/*
+		 * shmem_restore_parallel() already created the memfd and read
+		 * the contents into it; everything up to the handshake is
+		 * exactly that work.
+		 */
+		f = si->prep_fd;
+		si->prep_fd = -1;
+		goto content_ready;
+	}
+
 	if (kdat.has_memfd && (!is_hugetlb || kdat.has_memfd_hugetlb)) {
 		f = memfd_create("", memfd_flag);
 		if (f < 0) {
@@ -636,7 +921,9 @@ static int open_shmem(int pid, struct vma_area *vma)
 			goto err;
 	}
 	munmap(addr, si->size);
+	addr = MAP_FAILED;
 
+content_ready:
 	si->fd = f;
 
 	/* Send signal to slaves, that they can open fd for this shmem */
